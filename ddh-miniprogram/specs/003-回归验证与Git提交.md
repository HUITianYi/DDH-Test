# Spec 003：回归验证与 Git 提交

## 状态

待确认

## 前置条件

- `Spec 002：移除前置登录并保留历史记录按需登录` 已完成实施。

## 目标

验证本次整改是否满足审核要求，并在验证通过后完成 Git 提交准备。

## 本步骤内容

### 1. 回归验证

验证以下路径：

1. 小程序冷启动进入首页时，不强制登录。
2. 未登录用户可进入“新建筛查任务”页面。
3. 未登录用户可提交一次分析并查看当前报告。
4. 未登录用户点击“查看历史记录”时，才进入登录页。
5. 登录成功后，可进入历史记录页。
6. 已登录用户访问历史记录和历史详情链路正常。

### 2. 验证记录落档

将本次验证结论写入 spec 或补充记录，标记通过项、未验证项和风险项。

### 3. Git 提交

验证通过后，完成 Git 提交。

## Git 决策点

当前目录 `E:\DDHsystem\ddh-miniprogram` 不是 Git 仓库，因此本步骤需要你确认以下其中一种方式：

1. 提供正确的上层 Git 仓库路径，我在该仓库中提交。
2. 同意在当前目录初始化 Git 仓库后再提交。

## 验收标准

1. 回归结果表明本次整改满足“先浏览、后按需登录”的审核要求。
2. 已形成可复核的验证记录。
3. Git 提交方式已明确。
4. 在可提交的仓库内完成一次清晰的 commit。

## 待你确认

请确认是否进入本步骤，以及 Git 采用哪种方式处理。
